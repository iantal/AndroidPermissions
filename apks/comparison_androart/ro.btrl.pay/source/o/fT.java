package o;

import java.util.regex.Pattern;

public final class fT
{
  private static final Pattern ˋ = Pattern.compile("\\$\\{(.*?)\\}");
  
  public static boolean ˎ(String paramString)
  {
    return (paramString == null) || (paramString.trim().isEmpty());
  }
}
