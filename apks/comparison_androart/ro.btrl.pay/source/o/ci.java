package o;

import com.google.android.gms.common.api.Status;

public class ci
  extends Exception
{
  protected final Status ˋ;
  
  public ci(Status paramStatus)
  {
    super(String.valueOf(str).length() + 13 + i + ": " + str);
    this.ˋ = paramStatus;
  }
}
