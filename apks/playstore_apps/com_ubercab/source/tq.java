import android.view.ViewGroup;

class tq
{
  tq() {}
  
  public boolean a(ViewGroup paramViewGroup)
  {
    Boolean localBoolean = (Boolean)paramViewGroup.getTag(ai.tag_transition_group);
    return ((localBoolean != null) && (localBoolean.booleanValue())) || (paramViewGroup.getBackground() != null) || (tb.o(paramViewGroup) != null);
  }
}
