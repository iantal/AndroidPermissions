import android.content.SharedPreferences;
import java.util.concurrent.Callable;

final class dlg
  implements Callable<Boolean>
{
  dlg(SharedPreferences paramSharedPreferences, String paramString, Boolean paramBoolean) {}
}
