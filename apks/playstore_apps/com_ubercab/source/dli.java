import android.content.SharedPreferences;
import java.util.concurrent.Callable;

final class dli
  implements Callable<Integer>
{
  dli(SharedPreferences paramSharedPreferences, String paramString, Integer paramInteger) {}
}
