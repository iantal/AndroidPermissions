import android.content.Context;
import okhttp3.OkHttpClient;

public class bcp
{
  public static bew a(Context paramContext, OkHttpClient paramOkHttpClient)
  {
    return bev.a(paramContext).a(new bcq(paramOkHttpClient));
  }
}
